#include <stdio.h>

/**
 * main - writes its name to the command line
 * @argc: number of cmd args
 * @argv: array of cmd args
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
