#include <stdlib.h>
#include <string.h>
/**
 * strtow - will split a str into 0 word
 * @str: input str
 * Return: str or null on fail
 */
char **strtow(char *str)
{
	char **strnew;
	int len;

	if (str == NULL)
	{
		return (0);
	}
	len = strlen(str);
}
